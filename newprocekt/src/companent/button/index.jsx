import React from 'react'
import "./index.css"

const Mainbtn = (btn) => {
  return (
    <main>
    <button className={ a.bg === "pink" ? "blue pink" : "blue"}>
{a.btnText}
    </button>
</main>
  )
}

export default Mainbtn